# Create a BSL model object

Create a BSL model object

## Usage

``` r
bsl_create_model(dist, theta0)
```

## Arguments

- dist:

  Distribution name: `"lognormal"`, `"gamma"`, or `"weibull"`.

- theta0:

  Numeric vector of initial parameter values.

## Value

A BSL model object.
