% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{burr_phid_reliable}
\alias{burr_phid_reliable}
\title{Check whether Burr XII's per-study dispersion hierarchy is safe to attempt}
\usage{
burr_phid_reliable(
  datasets,
  stan_model,
  n_starts = 10,
  min_success_frac = 0.3,
  lp_agreement_tol = 1,
  verbose = TRUE
)
}
\arguments{
\item{datasets}{A named list of datasets as accepted by
\code{\link[=prepare_stan_data_from_datasets]{prepare_stan_data_from_datasets()}}.}

\item{stan_model}{The compiled hierarchical Burr XII model
(\code{dist_type = 4}), not the shared-phi variant.}

\item{n_starts}{Number of random-init \code{rstan::optimizing()} calls.}

\item{min_success_frac}{Minimum fraction of starts that must converge.}

\item{lp_agreement_tol}{Max log-posterior spread across successful starts
before they're judged to be different modes.}

\item{verbose}{Print a one-line verdict.}
}
\value{
\code{TRUE} if safe to attempt the per-study hierarchy, \code{FALSE} if it
should fall back to shared-phi. Always \code{TRUE} when there are no
mean+SD datasets (the constraint can't fire).
}
\description{
Burr XII's mean+SD likelihood needs \code{kappa*phi_d[d] > 4} per dataset (see
the Stan model's summary_type==3 branch); under the per-study phi_d
hierarchy this becomes n simultaneous constraints instead of one, which
can fragment the feasible region into separate modes. Detected here via a
cheap empirical stress test (several random-init \code{optimizing()} calls)
rather than a closed-form margin, which turned out unreliable in
practice.
}
