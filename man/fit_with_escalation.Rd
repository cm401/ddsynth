% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fit_with_escalation}
\alias{fit_with_escalation}
\title{Fit a Stan model with escalating settings, stopping once Rhat is acceptable}
\usage{
fit_with_escalation(
  stan_data,
  stan_model,
  rhat_target = 1.05,
  seed = 123,
  init = NULL,
  verbose = TRUE
)
}
\arguments{
\item{stan_data}{Data list for \code{rstan::sampling()}.}

\item{stan_model}{Compiled Stan model.}

\item{rhat_target}{Escalate if \code{max(Rhat)} exceeds this. Default 1.05,
matching the target agreed for the point 1/4 checkpoint run.}

\item{seed}{Passed to every tier for reproducibility.}

\item{init}{Optional init argument passed to every tier (e.g.
\code{make_stan_init_fn(stan_data)}); default \code{NULL} uses Stan's own default.}

\item{verbose}{If \code{TRUE} (default), reports which tier was used and why.}
}
\value{
A list: \code{fit} (the \code{stanfit} object from whichever tier
succeeded, or the last tier tried if none converged), \code{tier} (integer,
which tier succeeded or was last attempted), \code{max_rhat}, \code{divergences},
\code{converged} (logical), and \code{runtime_secs} (total across all tiers
attempted).
}
\description{
Tries a sequence of increasingly conservative sampling settings, stopping
at the first tier that achieves \code{max(Rhat) < rhat_target} with no
divergent transitions. Falls all the way back to the original
pre-lever-1 production settings (\code{iter=12000}, \code{adapt_delta=0.999},
\code{max_treedepth=12}) as the last tier if cheaper settings do not converge -
those settings were originally chosen because some chains did not
converge without them, so this is a floor, not just a starting point to relax.

Most fits are expected to converge at the cheap first tier, especially
now that lever 3 (\code{R/utils.R}'s \code{.order_stat_n_panels()}) has reduced
per-iteration cost and point 4's family gating means gamma no longer needs a
hierarchical \code{phi_d} funnel resolved. The escalation ladder exists for the
cases that still need it, not as the expected path.
}
