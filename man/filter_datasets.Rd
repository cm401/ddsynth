% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{filter_datasets}
\alias{filter_datasets}
\title{Filter a dataset list by subgroup and/or location}
\usage{
filter_datasets(datasets, subgroup = NULL, location = NULL)
}
\arguments{
\item{datasets}{A named list of datasets in the format accepted by
\code{\link[=prepare_stan_data_from_datasets]{prepare_stan_data_from_datasets()}}.  Each entry may optionally contain
\code{subgroup} and/or \code{location} character fields.}

\item{subgroup}{Character vector of subgroup values to retain, or \code{NULL}
(default) to skip subgroup filtering.}

\item{location}{Character vector of location values to retain, or \code{NULL}
(default) to skip location filtering.}
}
\value{
A named list containing only the datasets that satisfy all active
filter criteria.
}
\description{
Retains only those entries whose \code{subgroup} and/or \code{location} fields match
the requested values.  Entries that do not carry the field at all are
\strong{kept} when the corresponding filter argument is \code{NULL} and \strong{dropped}
when a filter is active (because their group membership is unknown).
Setting both arguments to \code{NULL} returns the list unchanged.
}
\examples{
datasets <- list(
  d1 = list(mean = 4.0, sd = 2.4, n = 49,
            subgroup = "tick-bite", location = "Turkey"),
  d2 = list(mean = 6.0, sd = 3.1, n = 12,
            subgroup = "nosocomial", location = "Iran"),
  d3 = list(mean = 5.0, sd = 2.0, n = 30)   # no subgroup/location
)
filter_datasets(datasets, subgroup = "tick-bite")
filter_datasets(datasets, location = c("Turkey", "Iran"))
filter_datasets(datasets, subgroup = "nosocomial", location = "Iran")

}
