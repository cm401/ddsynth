% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fit_corpus_parallel}
\alias{fit_corpus_parallel}
\title{Fit a list of (dataset, family) tasks concurrently, leaving cores free}
\usage{
fit_corpus_parallel(
  tasks,
  chains_per_fit = 4,
  reserve_cores = 4,
  rhat_target = 1.05,
  on_complete = NULL,
  task_timeout_secs = 90 * 60
)
}
\arguments{
\item{tasks}{A list of task specifications, each a list with:
\code{label} (character, for logging), \code{datasets} (named list, as passed to
\code{\link[=prepare_stan_data_from_datasets]{prepare_stan_data_from_datasets()}}), \code{dist_name} (one of "lognormal",
"gamma", "weibull", "burr", "gengamma"), \code{stan_model} (compiled model to
use for this task - the caller decides hierarchical vs. shared-phi per
family, see \code{analysis/main_analysis.R}'s \code{SHARED_PHI_FAMILIES}), and
\code{output_file} (path to save this task's result to).}

\item{chains_per_fit}{Chains used within each individual fit (passed to
\code{\link[=fit_with_escalation]{fit_with_escalation()}} tiers implicitly - currently fixed at 4 within
that function; this argument only affects the core-budget arithmetic
below, so keep it in sync if that changes).}

\item{reserve_cores}{Cores to leave free for other use on the machine.
Default 4 (out of this machine's 12), leaving meaningful headroom for
interactive use, not just background slack - do not reduce this without
explicit instruction.}

\item{rhat_target}{Passed through to \code{\link[=fit_with_escalation]{fit_with_escalation()}}.}

\item{on_complete}{Optional \verb{function(task, result)}, called in the
parent process as soon as each task finishes (\code{result} is the same list
already saved to \code{task$output_file}, or \code{list(error=...)} on failure).
Intended for incremental merge-and-save into a caller-side results
object, e.g. via \verb{<<-} from a top-level analysis script - see
\code{analysis/main_analysis.R} for the pattern. Errors inside the callback
are caught and reported, not allowed to take down the whole run.}

\item{task_timeout_secs}{Wall-clock ceiling per task (all escalation tiers
combined), default 5400 (90 min). Guards against a chain wandering into
a genuinely slow numerical region and grinding forever without
erroring. Past this, the task is killed and recorded with
\code{error = "timed out after <n>s"}.}
}
\value{
Invisibly, a character vector of the \code{output_file} paths written
(some may be missing if a task errored - check before merging).
}
\description{
Runs \code{\link[=fit_with_escalation]{fit_with_escalation()}} over a list of independent fitting tasks with
bounded concurrency (fork-based - Unix/macOS only), each task's chains
still parallelised internally as usual. Deliberately caps total core
usage well below the machine's full core count so the machine remains
usable for other work while a corpus run is in progress - this is a hard
requirement, not a tuning default: leave \code{reserve_cores} alone unless the
person running this has explicitly said otherwise.

Streams rather than batches: uses a manually-managed rolling pool
(\code{parallel::mcparallel()}/\code{mccollect()}, not \code{mclapply()}) so a finished
slot is immediately handed the next queued task instead of waiting for
the whole set dispatched together to finish - this both keeps the core
pool continuously busy (a straggler task no longer stalls an entire
batch) and lets \code{on_complete} fire the moment \emph{each} task finishes, not
just at the end. Each task's result is still also written to its own
file immediately on completion (\code{task$output_file}) regardless of
\code{on_complete}, so a crash partway through loses only the tasks that
hadn't finished yet, not everything; \code{\link[=merge_parallel_fit_results]{merge_parallel_fit_results()}} can
assemble those files after the fact if \code{on_complete} wasn't used to do it
incrementally.
}
