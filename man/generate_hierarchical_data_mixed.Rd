% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{generate_hierarchical_data_mixed}
\alias{generate_hierarchical_data_mixed}
\title{Generate data from hierarchical model with mixed summary types and sample sizes}
\usage{
generate_hierarchical_data_mixed(
  n_datasets,
  n_obs,
  dist_type = c("lognormal", "gamma", "weibull"),
  mu0,
  tau,
  phi,
  summary_type = NULL
)
}
\arguments{
\item{n_datasets}{Number of datasets to generate}

\item{n_obs}{Vector of sample sizes for each dataset (can vary)}

\item{dist_type}{Distribution type: "lognormal", "gamma", or "weibull"}

\item{mu0}{Population mean (location parameter)}

\item{tau}{Between-study standard deviation}

\item{phi}{Distribution-specific shape/scale parameter}

\item{summary_type}{Summary type specification. Can be: \code{NULL} (random mix of
types 1-3), a single integer 1-4 (all datasets use that type), a vector of
length \code{n_datasets} (one type per dataset), a length-3 probability vector
(sample from types 1-3 with those probabilities), or a length-4 probability
vector (sample from types 1-4 with those probabilities). Type 4 produces a
frequency table of observations rounded to the nearest whole day.}
}
\value{
List containing true parameters and observed summary statistics
}
\description{
Generate data from hierarchical model with mixed summary types and sample sizes
}
