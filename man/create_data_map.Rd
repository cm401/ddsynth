% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_utils.R
\name{create_data_map}
\alias{create_data_map}
\title{Create a global map of dataset geographic distribution}
\usage{
create_data_map(
  data = NULL,
  exclude_no_n = FALSE,
  default_n_log = 1.5,
  alpha = 0.6,
  jitter_amount = 3,
  title = "Geographic distribution of delay distribution datasets"
)
}
\arguments{
\item{data}{A data frame from \code{\link[=extract_dataset_summary]{extract_dataset_summary()}}. If \code{NULL},
\code{\link[=extract_dataset_summary]{extract_dataset_summary()}} is called internally.}

\item{exclude_no_n}{Logical. If \code{TRUE}, entries with missing sample size are
dropped. If \code{FALSE} (default), they appear at size \code{default_n_log}.}

\item{default_n_log}{Numeric. Log-scale dot size used when \code{n} is missing.}

\item{alpha}{Numeric (0–1). Point transparency.}

\item{jitter_amount}{Numeric. Maximum spatial jitter in degrees lat/lon,
applied to separate overlapping points from the same country.}

\item{title}{Character. Plot title.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Plots each dataset entry as a dot on a world map. Dot size is proportional
to \code{log(n+1)} and dot colour indicates pathogen. Datasets with
\code{country = "Mixed"} are excluded from the map and listed in an inset box.
}
\details{
For reproducible jitter, call \code{\link[=set.seed]{set.seed()}} before this function.
}
