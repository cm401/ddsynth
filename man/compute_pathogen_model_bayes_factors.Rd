% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ploting_utils.R
\name{compute_pathogen_model_bayes_factors}
\alias{compute_pathogen_model_bayes_factors}
\title{Compute LOO-based model weights for all pathogens}
\usage{
compute_pathogen_model_bayes_factors(all_results, analysis = "filtered")
}
\arguments{
\item{all_results}{Nested list produced by \code{analysis/main_analysis.R}.}

\item{analysis}{Character scalar.  Which analysis slot to use
(default \code{"filtered"}).}
}
\value{
A named list, one entry per pathogen.  Each entry is a named
numeric vector of model weights (summing to 1) sorted in decreasing
order, keyed by distribution name.  Pathogens with fewer than one valid
fit return \code{NULL}.
}
\description{
For each pathogen, computes Leave-One-Out cross-validation (LOO-CV) for
every valid distribution fit in the chosen \code{analysis} slot, then converts
the ELPD differences into pseudo-Bayes-factor model weights.

\strong{Why not bridge sampling?}  \code{bridgesampling::bridge_sampler()} requires
the compiled C++ Stan model to be present in memory.  When a \code{stanfit} is
saved to RDS and reloaded the internal model object is not serialised and
the call fails.  LOO-CV operates entirely on the stored MCMC draws
(\code{log_lik} parameter) so it works correctly on reloaded fits.

\strong{Method}: \code{loo::loo()} is called on the non-placeholder \code{log_lik} columns
of each fit (the Stan model stores zero-filled placeholder columns for
unused summary-statistic slots; these are stripped before passing to LOO).
The resulting ELPD estimates are converted to weights via softmax, giving
relative model probabilities under equal model priors — analogous to Bayes
factors computed from marginal likelihoods.
}
\seealso{
\code{\link[=plot_main_figure]{plot_main_figure()}}
}
