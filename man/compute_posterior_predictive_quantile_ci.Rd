% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{compute_posterior_predictive_quantile_ci}
\alias{compute_posterior_predictive_quantile_ci}
\title{Compute credible interval for posterior predictive quantiles}
\usage{
compute_posterior_predictive_quantile_ci(
  fit,
  dist_type,
  n_datasets,
  probs = c(0.5, 0.95),
  n_post_draws = 500L,
  n_mc_per_draw = 1000L
)
}
\arguments{
\item{fit}{A \code{stanfit} object.}

\item{dist_type}{Distribution type string.}

\item{n_datasets}{Number of studies in the fitted data (used to decide
whether to integrate over mu0/tau or use mean(loc_d)).}

\item{probs}{Probabilities for which predictive quantiles are computed.
Default \code{c(0.5, 0.95)}.}

\item{n_post_draws}{Number of posterior draws to use. Default 500.}

\item{n_mc_per_draw}{Number of predictive samples per posterior draw.
Default 1000.}
}
\value{
A list with elements \code{lower}, \code{median}, and \code{upper}:
the 2.5th, 50th, and 97.5th percentiles of the posterior distribution of
each quantile, each a numeric vector of length \code{length(probs)}.
}
\description{
For each requested probability \code{p}, draws from the posterior predictive
distribution and returns the 2.5th, 50th, and 97.5th percentiles of the
resulting quantile distribution across posterior draws.
}
