% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{merge_parallel_fit_results}
\alias{merge_parallel_fit_results}
\title{Assemble per-task .rds files from \code{\link[=fit_corpus_parallel]{fit_corpus_parallel()}} into one list}
\usage{
merge_parallel_fit_results(output_files, labels)
}
\arguments{
\item{output_files}{Character vector of file paths (as returned by
\code{\link[=fit_corpus_parallel]{fit_corpus_parallel()}}).}

\item{labels}{Character vector, same length, used as names in the
returned list (typically \code{"<pathogen>.<family>"}).}
}
\value{
A named list of the per-task results; entries for missing files
are \code{NULL} with a message, not a hard error, so a partially-complete
run can still be assembled and inspected.
}
\description{
Assemble per-task .rds files from \code{\link[=fit_corpus_parallel]{fit_corpus_parallel()}} into one list
}
